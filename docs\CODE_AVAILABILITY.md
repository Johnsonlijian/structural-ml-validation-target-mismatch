See the root `README.md`, `REPRODUCIBLE_RUNBOOK.md`, and `DATASETS_AND_LINKS.csv`.

This public repository contains code, configuration, source manifests, derived
tables, generated figures, and smoke tests. It intentionally excludes active
submission manuscripts, cover letters, reviewer-response drafts, private
round/log folders, local environments, credentials, and raw third-party datasets
whose redistribution terms are unclear or restricted.
