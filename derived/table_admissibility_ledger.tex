% auto-generated by analysis/108_close_panel_gaps.py
\begin{table}[htbp]
\centering
\caption{Admissibility ledger. The count of thirty-four executed splits, the six exclusions and the thirty-two distinct admissible entries that carry the paper's contrasts are three stages of one accounting chain, not three competing totals.}
\label{tab:S12}
\footnotesize
\begin{tabular}{lr p{6.1cm}}
\toprule
Stage & Count & What it counts \\
\midrule
Executed split configurations (dataset $\times$ variant) & 34 & every split for which folds were built and scored \\
Excluded: fold size outside $[L,U]$ & 6 & CFST family-honouring and its size-matched control (one fold holds 674 of 1{,}316 rows); the four wall proxy splits (folds of 2 and 111 rows against $[8,71]$) \\
Admissible split configurations & 28 & carry a performance claim \\
Executed learner-level contrasts & 48 & each admissible split $\times$ the learners run on its asset \\
Admissible contrasts & 36 & rows whose split is admissible \\
Distinct admissible entries & 32 & the concrete asset's eight-input key and its proxy label are the same evaluation, so four rows merge into two entries \\
\bottomrule
\end{tabular}
\end{table}